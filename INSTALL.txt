AJN surgical drop-in — channels.ts only

Replace ONLY the existing channels.ts with channels.ts from this ZIP.

Change:
- Adds isBrowserPlayable()
- Hard-excludes non-browser-playable Archive derivatives before candidate selection
- Removes isBrowserNative ranking/fallback selection
- Preserves existing metadata resolution, URL construction, proxy path, schedule, and skip behavior

Do NOT replace:
- server.ts
- MinimalPlayer.tsx
- App.tsx
- EPG
- scheduler
- navigation
- HLS
- news.json
- any other file

Verification:
1. npm run build
2. Test FOX News first, then CNN, MSNBC, BBC News.
3. Confirm successful activation contains the actual Archive filename.
4. Confirm unavailable/non-browser-playable items are skipped and do not activate the player.
