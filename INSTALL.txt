AJN ARCHIVE PROXY — RANGE STREAMING SURGICAL DROP-IN

Scope:
- Replace ONLY the existing server.ts with this server.ts.
- No other application files are changed.
- No EPG, navigation, player, channel, metadata, or scheduling changes.

Repair:
- Removes the active 2 MB upstream Range rewrite.
- Forwards browser Range headers exactly, including open-ended bytes=N-.
- Preserves upstream Content-Length and Content-Range.
- Streams the complete upstream response body without an artificial buffer clamp.
- Rejects malformed Range syntax with HTTP 416.
- Preserves the existing Archive path validation, retry behavior, media-content guard,
  client disconnect handling, and health/schedule/channel routes.

Phase 3 verification:
1. npm run build
2. Test bounded and open-ended ranges against a known-good Archive MP4.
3. Verify:
   - HTTP 206 for Range requests
   - Content-Type: video/mp4
   - Content-Range matches the requested range
   - Content-Length equals bytes actually delivered
   - bytes=2097152- and bytes=5000000- do NOT return 500
4. Test FOX -> CNN -> MSNBC -> BBC in the player.
5. Confirm first frame, advancing currentTime, audio, and no MEDIA_ELEMENT_ERROR code 4.

Do not declare PASS from build success alone.
