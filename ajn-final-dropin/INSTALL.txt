AJN Precision Engineering — Archive Media Repair FINAL DROP-IN v2

Replace these matching files in the current project:
  archive-discovery.ts
  channels.ts
  server.ts
  src/App.tsx
  src/MinimalPlayer.tsx

This revision keeps the prior media-resolution/proxy repairs and increases the
Archive metadata request timeout to 60 seconds in both resolver paths. It does
NOT introduce speculative media URLs when metadata cannot be resolved.

No other files are required.
